package com.qeetgroup.qeetpay.messaging;

/** Delivery channel for a message (via qeet-notify). WhatsApp is the differentiator (PRD Module 09). */
public enum MessageChannel {
    WHATSAPP,
    SMS,
    EMAIL
}
