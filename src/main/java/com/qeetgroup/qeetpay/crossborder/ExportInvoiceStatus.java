package com.qeetgroup.qeetpay.crossborder;

/** Export-invoice lifecycle: ISSUED until the foreign inward remittance settles it (REMITTED). */
public enum ExportInvoiceStatus {
    ISSUED,
    REMITTED
}
