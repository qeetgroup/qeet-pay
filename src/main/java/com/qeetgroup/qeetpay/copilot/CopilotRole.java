package com.qeetgroup.qeetpay.copilot;

/** Who authored a {@link CopilotMessage}: the merchant ({@code USER}) or the copilot ({@code ASSISTANT}). */
public enum CopilotRole {
    USER,
    ASSISTANT
}
