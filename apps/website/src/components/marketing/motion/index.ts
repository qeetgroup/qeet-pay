/**
 * Marketing motion kit — small, typed, reduced-motion-safe primitives built on
 * `motion/react`. Every component renders a static fallback (no transform /
 * opacity animation) when the user prefers reduced motion. All are client
 * components (`"use client"`).
 */
export { FadeRise } from "./fade-rise";
export { MagneticButton } from "./magnetic";
export { Parallax } from "./parallax";
export { Reveal } from "./reveal";
export { ScrollProgress } from "./scroll-progress";
export { Stagger, StaggerItem } from "./stagger";
export { Tilt } from "./tilt";
export { WordReveal } from "./word-reveal";
